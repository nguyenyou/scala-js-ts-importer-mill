//| mill-version: 1.0.2

import mill.*, scalalib.*

object tsimporter extends ScalaModule {
  def scalaVersion = "2.12.20"
  def mvnDeps = Seq(
    mvn"org.scala-lang.modules::scala-parser-combinators:1.1.2",
    mvn"com.github.scopt::scopt:3.7.1",
    mvn"org.scalatest::scalatest:3.0.5"
  )

  object test extends ScalaTests {
    def mvnDeps = Seq(mvn"com.lihaoyi::utest:0.9.0")
    def testFramework = "utest.runner.Framework"
  }
}